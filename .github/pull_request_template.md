## Problem
<!-- What issue does this PR solve? -->

## Why
<!-- Why is this change needed? Root cause, product reason, or risk being reduced. -->

## What changed
<!-- List the key changes. Surfaces affected (API / CLI / MCP / doctor / UI). -->

## Validation
<!-- How was this tested? -->
- [ ] `cargo fmt --all -- --check`
- [ ] `cargo clippy --workspace --all-targets --locked`
- [ ] `cargo test --workspace --locked`

## Impact
<!-- What crates/repos are affected by this change? -->

## Breaking changes
<!-- Does this require a version bump in downstream crates? If SDK types changed, all consumers must update. -->
